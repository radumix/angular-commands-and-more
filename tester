html inspect 


  import { test, expect, chromium } from '@playwright/test';

test('check no cookies in incognito', async () => {
  const browser = await chromium.launch({ headless: false }); // Runs in headful mode
  
  const context = await browser.newContext();
  const page = await context.newPage();
  await page.goto('https://example.com');
  
  const cookies = await context.cookies();
  expect(cookies.length).toBe(0);
  
  const localStorageData = await page.evaluate(() => localStorage.length);
  expect(localStorageData).toBe(0);
  
  await browser.close();
});




