html inspect 

import { test, expect, chromium } from '@playwright/test';

test('incognito test', async () => {
  const browser = await chromium.launch(); // Launch browser
  const context = await browser.newContext(); // Create a new incognito context
  const page = await context.newPage(); // Open a new page in the incognito context

  await page.goto('https://example.com');
  expect(await page.title()).toBe('Example Domain');

  await browser.close();
});

